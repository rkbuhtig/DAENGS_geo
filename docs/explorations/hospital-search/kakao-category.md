---
status: rejected
depends-on: 카카오 REST 키
---
# 카카오 로컬 카테고리 보강

> **결정 #64 로 기각.** 이 갈래가 보강하려던 것은 `specialty` 인데 그 축이 없어졌다.
> 한국 수의 진료에 과목 제도가 없어서 `category_name` 의 "> 안과" 도 자격이 아니라
> 업소가 고른 분류다. 없는 축을 남의 데이터로 채워도 없는 축이다.

카카오 로컬 키워드/카테고리 검색 응답의 `category_name` = `"의료,건강 > 동물병원 > 안과"` 식. x/y/radius 지원, 15건×45페이지, 일 10만.

- 요청 시 조회 → 우리 DB 행과 이름+50m 매칭 → `specialty`, `place_url`, `phone` 응답에 실음. **저장 안 함** (약관)
- 네이버 지역검색은 5건 제한이라 이 용도엔 카카오만 실질적 → 지도 SDK가 뭐든 **보강 검색은 카카오**
- 이름 검색이라 소개문("대형견 환영")은 안 걸림
