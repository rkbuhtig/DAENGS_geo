# 견주 프로필 계약 + 가상 페르소나

개 프로필과 마찬가지로 원천은 **타 담당**. 여기선 소비만 한다. → [dog-profile.md](dog-profile.md)

## 페르소나를 가르는 축: **병원의 역할을 아는 정도**

주인은 **어떤 병원이 뭘 하는 곳인지 모르는 채로** 온다. 1차와 2차가 다르다는 것, '24시'와 '응급'이 다르다는 것,
안과·정형이 따로 있다는 것 — 이걸 모르는 상태에서 "우리 개가 이상하다"만 들고 들어온다.
그 무지를 서비스가 대신 넘어주는 게 이 기능의 값이다.

그래서 페르소나 축은 인구통계(나이·성별·소득)가 아니라 **무지의 종류 × 이동 제약**이다.

| 견주 | 개 | 아는 정도 | **모르는 것** | 서비스에 기대하는 것 |
|---|---|---|---|---|
| **지현** | 삼월 | `none` | **뭘 모르는지도 모른다.** 동물병원이 다 같은 줄 안다. 접종 전엔 땅에 못 내려놓는다는 것도 방금 알았다 | 증상을 문장으로 던지면 조건으로 번역해 주는 것. 되묻기(`ask`)가 자주 발동해야 정상 |
| **명수** | 할매 | `basic` | **'24시'와 '응급'이 다르다는 것.** 새벽 3시, 지금 문 연 곳이 어딘지만 필요 | 클릭 최소. `open_now`+`urgency` 적용, 전화번호가 1급 정보. 차 없어 택시뿐 |
| **은영** | 장군, 초코 | `experienced` | **없다.** 2차·전문의·과목을 안다. 병원을 지목할 수도 있다 | 모르는 건 하나뿐 — **"거기까지 우리 개가 갈 수 있나."** 이 사람에겐 `journey`가 본체다 |
| **태우** | 두부, 뽀글 | `basic` | **도보권에 뭐가 있는지.** 큰 병원은 멀다는 것만 안다 | 반경·도보 시간이 결정적. 대중교통 동반 가능 여부 |
| **서준** | 콩이, 바우 | `basic` | 대형견을 받아주는 곳이 따로 있는지 | 차는 있는데 바우(32kg)를 못 안는다 — 주차·통로가 실제 제약 |

### 못 푸는 페르소나 (범위 밖, 지우지 않고 남김)

**"비용 걱정형"** — 진료비를 모르고 과잉진료를 걱정한다. 경쟁 조사에서 가장 크게 잡힌 수요다
([competitor-reviews](../research/2026-08-19-competitor-reviews.md): 마이펫플러스·펫트라슈가 파는 게 정확히 이것).

우리는 **진료비 데이터가 없고 만들 계획도 없다.** 영수증 수집은 다른 사업이고, 제휴 병원 가격만 보여주면
같은 조사에서 "광고판·장사꾼"으로 읽힌 그 모델이 된다. 그래서 이 페르소나는 **의도적으로 안 푼다.**
`rejected` 갈래를 안 지우는 것과 같은 이유로 여기 남긴다 — 같은 질문을 다시 안 하려고.

## 인터페이스 (수신 형태 가정)

필드마다 **바꾸는 판정이 하나씩** 있다. 그게 없는 축은 넣지 않았다.

```
owner_id, name
dog_ids        []       이 사람이 보는 개들

has_car        bool?    이동수단 선택지. 없으면 응급에 택시·도보뿐   → journey mode 기본값
can_carry_kg   float?   안고 오를 수 있는 무게                      → 계단이 진짜 장벽인지 판정
transit_ok     bool?    대중교통 동반 의사                          → journey 대중교통 노출
vet_literacy   enum?    none / basic / experienced                  → reply 상세도, ask 발동 임계
```

`None`은 **모름**이지 부정이 아니다. `can_carry(dog)`도 재료가 없으면 `None`을 낸다 — 판정을 지어내지 않는다.

**`has_car`는 원래 `DogProfile`에 있었다.** 개 속성인 척 잘못 붙어 있던 것을 여기로 옮겼다.
기존 필드는 deprecated로 남겨 `profile_hint`가 깨지지 않게 했다.

### 왜 이 필드들만인가

- **`can_carry_kg`** — 지금 `spots.py`는 소형견이면 무조건 "계단 — 안고 이동"이라고 한다. 주인이 못 안으면
  그건 조언이 아니라 소음이다. 장군(34kg)·바우(32kg)에서 바로 갈린다.
- **`transit_ok`** — 대중교통 노출은 현재 개 크기 + 견주 의사의 합으로 resolver에 배선됐다.
  은영은 차가 있어서 안 타고, 태우는 차가 없어서 타야 한다 — 둘 다 개 크기와 무관한 이유다.
- **`vet_literacy`** — 지현에게 "2차 병원 위주로 좁혔습니다"는 아무 의미가 없다. 같은 `changes`도 사람에 따라
  풀어 써야 한다. LLM이 정보를 만드는 게 아니라 **이미 확정된 변경을 다른 어휘로 말하는** 것이므로 원칙에 안 어긋난다.

### 의도적으로 없는 것

- **비용 민감도** — 진료비 데이터가 없다 (위 참조)
- **입력 선호(음성/타이핑)** — 클라이언트가 아는 것이고 서버 판정을 안 바꾼다. 페르소나 서술로만 남긴다
- **거주지 주소** — 좌표는 요청마다 온다. 저장할 이유가 없다

## 배선 상태

`OwnerProfile` 조회와 `transit_ok`는 현재 병원·journey resolver에 배선됐다. 나머지 필드는 계약과
테스트는 있으나 실제 판정에는 아직 일부만 연결돼 있다.

| 붙일 곳 | 지금 | 붙이면 |
|---|---|---|
| `planning.resolver._available_modes` | **배선 완료**: 개 크기 + `owner.transit_ok` | 실제 외부 프로필 원천 연결 |
| `journey.advice.walk_advice` | `can_carry()` 계약·테스트만 있고 advice 미배선 | True면 계단 장벽 완화 |
| `refine.engine.draft` | 개 프로필로 도보 옵션 기본값만. `OwnerProfile.has_car` 미배선 | mode 기본값 정책을 채택할 때 연결 |
