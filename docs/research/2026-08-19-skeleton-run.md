# 워킹 스켈레톤 실행 로그 (2026-08-19)

키 0개, PostGIS 시드 9건(고양이전문 1 포함), 페르소나 3마리. `POST /hospital/search`.

| 시나리오 | 결과 |
|---|---|
| 할매 · 메뉴 진입 | 초안 반경 2km 필터 없음. 고양이전문·송파(반경 밖) 제외. 도보 옵션 기본 `no_stairs`(프로필). 거리순 6곳 |
| 할매 · "눈이 뿌옇고 자꾸 부딪혀요 걸어서 갈 수 있는 데" | `set_specialty[eye]`, `set_mode walk` → 정렬 소요시간순. 안과 태그·근거 있는 곳 boost. advice: 15분 넘는 곳 전부 avoid(노령·관절 권장 ≤15분) |
| 할매 · UI `set_walk_max 15` | changes "도보 최대 15분", why에 "> 제한 15분" 추가 |
| 할매 · "첫번째 거는 빼줘" | shown_ids[0] 제외 |
| 할매 · "아까대로" | undo → 제외 복구. (수정 전엔 "대로"가 큰길 규칙에도 걸렸음 → undo/reset 단독 적용으로 고침) |
| 두부 · 딥링크(야간+영업중, 23시) + "차 없어요 택시로" | 3곳(24시 2 + 서초야간 자정넘김). `set_mode car`, 택시비 표시. small이라 transit 칸 있음 |
| 콩이 · "뒷다리를 절뚝거려요" | `set_specialty[ortho]`. medium이라 transit 없음. 부스트는 같은 500m 밴드 안에서만 순서 변경 |
| 콩이 · "음 글쎄" | 툴 없음 → `question` 되물음, 상태 불변 |

## 고친 것
- 부스트가 거리를 뒤집던 문제 → 밴드(500m/5분) 내 재정렬로 제한. "살짝 위"
- 초안에 근거가 붙어 무관한 부스트 → 발화·특화 있을 때만 근거 검색

## 다음 (키 오면)
- TMAP `route()` 실호출 검증, `facilities` 실값
- 네이버 검색 API → `NaverCommunitySearch`, 병원명 매칭을 LLM 선택으로
- OpenAILLM 실호출, FakeLLM은 1차 규칙 필터로 유지
