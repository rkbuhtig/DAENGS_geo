package com.daengs.geo.map.layers.trail

/** Map rendering keeps its old imports while recording ownership moves into the walk domain. */
typealias TrackingState = com.daengs.geo.walk.TrackingState
typealias TrailSnapshot = com.daengs.geo.walk.TrailSnapshot
